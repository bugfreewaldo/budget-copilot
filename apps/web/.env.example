# Next.js App Configuration
NEXT_PUBLIC_APP_NAME="Budget Copilot"

# Database (libSQL server - self-hosted sqld exposed via tunnel)
LIBSQL_URL="http://localhost:8080"
# LIBSQL_AUTH_TOKEN="" # Optional: set if your sqld requires auth

# AI (Claude) - Required for Budget Copilot AI features
ANTHROPIC_API_KEY="sk-ant-..."
